# [days] 메모앱 개발가이드

## 프로젝트 개요

### 기본 정보
- **주제**: PWA 기반 오프라인 메모장 애플리케이션
- **프로젝트명**: days
- **배포 URL**: https://days-green.vercel.app/

### 목적
- 일별로 메모하는 메모앱으로 시각적인 효과로 중요도와 스케줄의 흐름을 보여주는 즐거운 앱

### 타깃 사용자
- 형식없는 것에 불편하지 않으면서도 지킬것을 지키는 사람

### 톤 & 비주얼 컨셉
- 하늘과 산들바람에 흘러가는 뭉게구름
- 메모는 하늘에 떠다니는 "뭉게구름 동그라미"로 표현. 중요도가 높을수록 구름이 크고, 아직 실현되지 않은 계획은 점선(옅은 윤곽), 이미 지나간 날은 회색으로 흐려진다.

### 하지 않을 것 (Non-goals) — 잠정, 진행하며 조정 가능
- 제목/본문 검색 & 필터 기능 (2026-07-28 사용자 결정 — 진행 안 함)

## 작업 규칙

### 1. 승인 원칙
- "시작" 또는 "go"라는 말이 나오기 전까지는 코드를 읽고 분석만 하고, 실제로 파일을 만들거나 수정하지 않을 것.
- 여기서 "단계"는 1~9단계 같은 큰 번호 순서가 아니라 **하나의 완결된 기능 단위**(예: 헤더, 슬라이더, 더보기 메뉴의 "내보내기" 기능 등)를 뜻함.
  - 기능 하나가 UI(예: 3단계)와 실제 동작 로직(예: 5단계)처럼 서로 다른 번호의 체크리스트에 걸쳐 있을 수 있음. 이런 경우 UI만 먼저 만들고 로직은 나중에 따로 만들지 않고, **기능이 실제로 동작하도록 관련 항목을 한 번에 묶어서** 진행하는 것을 기본으로 함 (재작업 비용을 줄이기 위함).
  - 다만 서로 다른 기능을 한 세션에 여러 개 묶어 진행하는 것은 여전히 금지 (절대 금지 사항 참고).
- 승인은 **세션 전체에 한 번이 아니라, 기능 단위마다 매번** 받을 것.
- 한 기능이 끝나면 자동으로 다음 기능으로 넘어가지 말고, 반드시 새로 "시작"/"go"를 기다릴 것.
- 작업을 시작하기 전, 다음 형식으로 계획을 먼저 설명할 것.
  > "이번 단계에서는 [무엇을] 위해 [어떤 파일들]을 [어떻게] 수정하겠습니다."
  - 하나의 기능이 여러 번호의 체크리스트 항목을 함께 완료시키는 경우, 계획 설명 시 어느 항목들이 함께 체크되는지 명시할 것.

### 2. 작업 기록 규칙 (이전 작업 기억하기)
- 새 세션을 시작할 때 먼저 `PROGRESS.md` 파일이 있는지 확인하고, 있으면 내용을 읽을 것.
- 가장 마지막에 기록된 작업 내용을 요약해서 사용자에게 보고할 것.
- "다음 작업 제안"이 적혀 있으면 함께 보고할 것.
- `PROGRESS.md`가 없으면 "이전 작업 기록이 없습니다"라고 알릴 것.
- `PROGRESS.md` 맨 아래에 기록을 추가할 것 (기존 기록은 지우지 말고 누적할 것).
- 작업 순서는 체크리스트를 확인하여 순서대로 진행하되, 번호 순서는 권장 순서일 뿐 절대 규칙은 아님 — 진행 중인 기능이 다른 번호의 항목과 연결되어 있다면 그 항목까지 함께 진행 가능 (위 "1. 승인 원칙" 참고)
- 진행 완료된 체크리스트에 체크 표시 할 것
- 작업 전 항상 계획을 먼저 설명하고 승인을 받을 것
- 파일 수정 시 변경 내용을 먼저 요약할 것
- 각 작업 완료 후 CLAUDE.md 체크리스트를 반드시 업데이트 할 것
- [x] 처리 시 완료 날짜를 괄호로 표기할 것. 예: - [x] 로그인 UI (2026-04-01)

### 3. 검증 규칙 (기능 구현 후 확인)
- 자동화 테스트(로컬 서버/헤드리스 브라우저 등, `localhost`·`file://` 기준)를 통과했다고 해서 실제 환경에서도 정상 동작한다고 단정하지 말 것.
- 기능 구현 완료 후 확인은 2단계로 나눠 진행할 것.
  1. Claude가 자동화 테스트로 1차 확인
  2. 사용자가 실제 기기/네트워크 환경(폰 브라우저, 필요 시 PC를 LAN IP로 접속 등)에서 2차 확인
- 2차 확인이 끝나기 전까지는 다음 기능 단계로 넘어가지 말 것.

## 절대 금지 사항
- 승인 없이 파일 삭제 금지
- 여러 기능을 한 번에 진행 금지 (단, 하나의 기능이 여러 번호의 체크리스트 항목에 걸쳐 있는 경우는 예외 — 위 "1. 승인 원칙" 참고)
- 체크리스트에 없는 기능 임의 추가 금지
- 라이브러리 / 프레임워크 임의 설치 금지

---

## 기술 스택
- Frontend: HTML5, CSS3, Vanilla JavaScript (ES Modules)
- PWA: Service Worker, Web App Manifest
- Storage: localStorage (메모 데이터 영속 저장)
- Backup: JSON 파일 내보내기/가져오기
- 배포: Vercel
- 버전관리: GitHub

---

## 디자인 기획

### 컬러 시스템

> 배색 원칙: **본문 텍스트는 중립 잉크(ink), UI 텍스트는 하늘색(sky) 계열, 포인트(amber)는 화면의 5%만.** 포커스 링은 `sky 500` 이상.

#### Brand Colors
- **Sky (주조색)**: 하늘·배경·UI 텍스트 — 앱의 바탕이 되는 하늘색 계열
- **Amber (포인트)**: 버튼·태그·액센트 — 화면의 5%만 사용
- **Ink (중립)**: 사용자가 쓴 글(본문)에 사용
- **Danger (예외)**: 삭제·오류 전용 안전장치

#### Light Mode
아래 값은 컬러칩 원본 그대로이며, **변수명·값 임의 변경 금지**.

**배경 & 카드** — 화면의 바탕이 되는 면
| 이름 | HEX | 용도 |
|---|---|---|
| sky 25 | `#F4F8FC` | 더 연한 배경 |
| sky bg | `#E8F1FA` | 앱 배경 |
| sky 75 | `#D8E7F5` | 선택된 행 |
| card | `#FFFFFF` | 카드 표면 |

**하늘 · 텍스트** — UI가 말하는 부분에 사용
| 이름 | HEX | 용도 |
|---|---|---|
| sky 900 | `#16283A` | 최고 대비 |
| sky 800 | `#1F3A54` | 헤딩 · 날짜 |
| sky deep | `#1F3244` | 헤딩 대체 |
| sky 700 | `#2F5375` | 소제목 · 강조 |
| sky 600 | `#416B94` | 메타 · 링크 |
| sky sub | `#5D6E7E` | 배경 위 보조 |
| sky 500 | `#5A83AC` | 큰 글씨 · 포커스 |

**하늘 · 구분선** — 면을 나누는 얇은 선
| 이름 | HEX | 용도 |
|---|---|---|
| sky 200 | `#B2CDE6` | 섹션 구분 |
| sky 150 | `#CBDEEF` | 기본 구분선 |
| sky 100 | `#DCE9F5` | 은은한 선 |
| card line | `#E3EAF2` | 카드 테두리 |
| card line soft | `#EDF2F7` | 행 구분 |

**중립 잉크** — 사용자가 쓴 글에 사용
| 이름 | HEX | 용도 |
|---|---|---|
| ink 900 | `#23282E` | 본문 |
| ink 700 | `#55606C` | 보조 텍스트 |
| ink 500 | `#64707E` | 메타 |
| ink 200 | `#E3E8ED` | 비활성 |

**포인트 · 앰버** — 화면의 5%만 차지
| 이름 | HEX | 용도 |
|---|---|---|
| amber 900 | `#4A2E05` | 버튼 글씨 |
| amber 700 | `#7A4E0C` | 태그 글씨 |
| amber 500 | `#E8A33A` | 액센트 바 |
| amber 300 | `#F0A93C` | 버튼 배경 |
| amber 50 | `#FBEEDA` | 태그 배경 |

**핑크 · 캘린더 선택 강조** — 한줄 캘린더의 선택(오늘) 날짜 강조 전용 (참고: `reference/핑크.png`)
| 이름 | HEX | 용도 |
|---|---|---|
| pink 100 | `#FCE7EF` | 가장 옅은 배경 · 선택된 날짜 배경 |
| pink 200 | `#F8D3E1` | 태그 · 구분 (sky 150 대응) |
| pink 300 | `#F3B4CD` | 보이는 파스텔 |
| candy 500 | `#F2609A` | 메인 포인트 · CTA |
| candy 600 | `#DE4A85` | hover · 진한 텍스트 |

**예외** — 배색 원칙 밖의 안전장치
| 이름 | HEX | 용도 |
|---|---|---|
| danger | `#D93A3A` | 삭제 · 오류 |

**그림자**
- card-shadow: `0 1px 3px rgba(38, 90, 140, 0.08)`

#### Dark Mode
> ⚠️ **제안(초안)** — 원본 컬러칩에는 라이트 모드만 정의되어 있음. 아래는 라이트 팔레트를 기준으로 배경/잉크 관계를 반전시킨 시작점이며, 다크모드 기능 구현 단계(3단계)에서 실기기 확인 후 최종 값 확정 예정. amber 액센트 계열은 라이트와 공유.

| 역할(CSS 변수) | 제안 값 | 대응(라이트) |
|---|---|---|
| --bg (앱 배경) | `#0E1A26` | sky bg |
| --bg-soft (연한 배경) | `#122334` | sky 25 |
| --surface (카드) | `#182B3D` | card |
| --row-selected (선택된 행) | `#20374C` | sky 75 |
| --line (구분선) | `#243B52` | sky 100 |
| --line-strong (카드 테두리) | `#2E4A63` | card line |
| --ui-text (UI 텍스트) | `#B7CBDE` | sky 600 |
| --ui-text-strong | `#DDE9F4` | sky 900 |
| --ink (본문) | `#E3E8ED` | ink 900 |
| --ink-sub (보조) | `#A5B0BC` | ink 700 |
| amber(포인트) | 라이트와 동일 | amber 500/300 |

#### CSS 변수 선언 규칙
- 모든 컬러는 `css/variables.css`의 `:root`에 CSS 변수로 선언하고, **변수명은 컬러칩 이름을 kebab-case로 매핑**한다 (임의 변경 금지).
- 팔레트 변수(`--sky-500` 등)와 역할 변수(`--bg`, `--ink`, `--ui-text` 등)를 함께 둔다. 컴포넌트 CSS에서는 **역할 변수만 참조**하고 팔레트 변수를 직접 쓰지 않는다 (다크모드 전환을 역할 변수 재정의만으로 처리하기 위함).
- 다크모드는 `<html data-theme="dark">` 속성으로 전환하며, 다크 값은 `[data-theme="dark"]` 블록에서 역할 변수만 재정의한다.

```css
:root {
  /* ── 팔레트 (컬러칩 원본, 변경 금지) ── */
  /* 배경 & 카드 */
  --sky-25: #F4F8FC;
  --sky-bg: #E8F1FA;
  --sky-75: #D8E7F5;
  --card: #FFFFFF;
  /* 하늘·텍스트 */
  --sky-900: #16283A;
  --sky-800: #1F3A54;
  --sky-deep: #1F3244;
  --sky-700: #2F5375;
  --sky-600: #416B94;
  --sky-sub: #5D6E7E;
  --sky-500: #5A83AC;
  /* 하늘·구분선 */
  --sky-200: #B2CDE6;
  --sky-150: #CBDEEF;
  --sky-100: #DCE9F5;
  --card-line: #E3EAF2;
  --card-line-soft: #EDF2F7;
  /* 중립 잉크 */
  --ink-900: #23282E;
  --ink-700: #55606C;
  --ink-500: #64707E;
  --ink-200: #E3E8ED;
  /* 포인트·앰버 */
  --amber-900: #4A2E05;
  --amber-700: #7A4E0C;
  --amber-500: #E8A33A;
  --amber-300: #F0A93C;
  --amber-50: #FBEEDA;
  /* 핑크·캘린더 선택 강조 */
  --pink-100: #FCE7EF;
  --pink-200: #F8D3E1;
  --pink-300: #F3B4CD;
  --candy-500: #F2609A;
  --candy-600: #DE4A85;
  /* 예외 */
  --danger: #D93A3A;
  /* 그림자 */
  --card-shadow: 0 1px 3px rgba(38, 90, 140, 0.08);

  /* ── 역할 변수 (컴포넌트는 이것만 참조) ── */
  --bg: var(--sky-bg);
  --bg-soft: var(--sky-25);
  --surface: var(--card);
  --row-selected: var(--sky-75);
  --line: var(--sky-100);
  --line-strong: var(--card-line);
  --ui-text: var(--sky-600);
  --ui-text-strong: var(--sky-900);
  --heading: var(--sky-800);
  --ink: var(--ink-900);
  --ink-sub: var(--ink-700);
  --ink-meta: var(--ink-500);
  --ink-disabled: var(--ink-200);
  --accent: var(--amber-500);
  --btn-bg: var(--amber-300);
  --btn-text: var(--amber-900);
  --tag-bg: var(--amber-50);
  --tag-text: var(--amber-700);
  --focus-ring: var(--sky-500);
}

/* 다크모드 — 제안값, 역할 변수만 재정의 */
[data-theme="dark"] {
  --bg: #0E1A26;
  --bg-soft: #122334;
  --surface: #182B3D;
  --row-selected: #20374C;
  --line: #243B52;
  --line-strong: #2E4A63;
  --ui-text: #B7CBDE;
  --ui-text-strong: #DDE9F4;
  --heading: #DDE9F4;
  --ink: #E3E8ED;
  --ink-sub: #A5B0BC;
}
```

### 타이포그래피
> ⚠️ **미착수** — 폰트 이름(Pretendard)은 `css/variables.css`의 `--font-family`에 변수로만 선언돼 있고, 실제 `@font-face`/로컬 woff2 파일은 아직 추가하지 않음. 지금은 fallback인 시스템 폰트(`-apple-system` 등)로 렌더링되는 상태. 필요해질 때 아래 방식으로 구현.

- **본문/UI**: Pretendard (한글·영문 공용), fallback `-apple-system, "Apple SD Gothic Neo", "Malgun Gothic", sans-serif`
- 웹폰트는 **오프라인 PWA 특성상 로컬 번들 우선**(CDN 의존 금지). `assets/fonts/`에 subset(woff2) 포함 후 `@font-face`.
- 타입 스케일(제안): 헤딩 20px / 소제목 16px / 본문 15px / 메타 13px, line-height 1.6
- 메모 원(뭉게구름) 안 제목은 원 크기에 비례해 조정(작은 원 13px ~ 큰 원 18px).

### 아이콘
> ⚠️ **미착수** — 아직 어디에도 적용된 곳 없음. 지금까지는 텍스트("+", "← 목록으로" 등)로만 구현. 필요해질 때 아래 방식으로 구현.

- **라이브러리**: Lucide — **단, 스택이 Vanilla JS 이므로 `lucide-react`가 아니라 SVG 인라인 방식** 사용.
  - 필요한 아이콘의 Lucide SVG 원본을 `js/ui/icons.js`에 인라인 상수로 보관하고 DOM에 주입.
  - 이유: ① 프레임워크 없이 동작, ② CDN 없이 오프라인 보장, ③ "라이브러리 임의 설치 금지" 규칙 준수.

### UI 레이아웃 규칙

**공통 헤더 — "한줄 캘린더"**
- 모든 화면 상단에 고정. 날짜의 흐름을 한 줄로 보여주는 헤더.
- 상단 줄: 왼쪽에 "days" 로고.
- **날짜 셀을 탭하면 "선택된 날짜"로 표시**(오늘 표시용 배경색과는 별개로 테두리 링으로 구분). 선택 안 하면 기본값은 오늘.
- 새 메모 추가는 헤더가 아니라 **오른쪽 하단 플로팅 `+` 버튼(FAB)** — 메인화면에서만 보이고 상세보기에서는 숨김. 누르면 **캘린더에서 선택된 날짜**로 상세보기를 편집 모드로 바로 열어 작성 (미래 날짜 선택 후 작성 가능).

**메인 화면 (참고: 메인화면.png)**
- 메모는 **떠다니는 동그라미(뭉게구름)** 로 배치. 잔잔하게 부유하는 느낌(과하지 않은 애니메이션).
- **원 크기 = 중요도(size)**. 값이 클수록 원이 크다.
- **선 스타일 = 확정 상태(3단계, `status`, 참고: `reference/동그라미.png`)**: `unlikely`(가능성 없는 메모, 회색 채움) / `thinking`(생각중인 메모, 점선) / `confirmed`(확정된 메모, 실선).
- **색 = 시점**: 오늘/예정 메모는 진한 하늘 잉크, **이미 지나간 날짜는 회색**으로 흐려짐(원래 스타일 형태는 유지).
- 원 안에는 **제목만** 표시.
- **원은 손으로 드래그해 위치를 옮길 수 있다** (자세한 규칙은 아래 "드래그 상호작용" 참고).
- **원을 두 손가락으로 꼬집으면(핀치) 크기(중요도)가 바로 조절된다** — 다른 메모들과 화면에서 나란히 비교하면서 크기를 정할 수 있도록 상세보기가 아니라 메인화면에서 직접 조절. 최대 크기는 화면을 넘칠 만큼 크게 설정돼 있고, 핀치 중에는 브라우저 자체의 페이지 확대/축소가 절대 발생하지 않도록 원천 차단돼 있다.
- **배경(원이 아닌 빈 곳)을 한 손가락으로 좌우로 밀면 화면 전체가 패닝된다** — 화면 폭 기준 좌우로 아주 넓게(약 50배) 이동 가능해, 원을 화면 밖으로 밀어 치워두고 나중에 다시 밀어서 찾아올 수 있다. 상하 패닝은 없음(좌우만). 새로 만든 메모는 항상 처음 보이는 "홈" 영역에 생성되어 바로 보인다.

**드래그 상호작용 (라이브러리 없이 Pointer Events API 사용)**
- 마우스·터치·펜을 `pointerdown` / `pointermove` / `pointerup` 한 세트로 공용 처리 (별도 라이브러리 설치 금지 규칙 준수).
- 드래그 대상 원에는 CSS `touch-action: none`을 반드시 지정 — 없으면 모바일에서 원을 끌 때 화면이 함께 스크롤돼 드래그가 씹힌다.
- `pointerdown` 시 `setPointerCapture(pointerId)`로 포인터를 캡처 — 손가락이 원 밖으로 벗어나도 추적을 놓치지 않게 한다.
- 위치 이동은 `left/top`이 아니라 **`transform: translate`**로 처리 (리페인트 감소, 부드러움).
- **부유(뭉게구름) 애니메이션과 조율**: 두 효과 모두 `transform`을 건드리므로, `pointerdown` 때 원에 `.dragging` 클래스를 붙여 부유 애니메이션을 잠시 멈추고, `pointerup` 때 클래스를 제거해 다시 켠다.
- **위치 저장**: 드래그가 끝나면(`pointerup`) 좌표를 메모에 반영하고 `store.save()`로 localStorage에 저장. 좌표는 **절대 px가 아니라 컨테이너 대비 %(`xPct`, `yPct`)로 저장**해 기기 화면 크기가 달라도 위치가 깨지지 않게 한다.
- **탭 vs 드래그 구분**: 이동 거리가 임계값(예: 6px) 미만이면 "탭"(미리보기 열기), 이상이면 "드래그"(위치 이동)로 처리해 오작동을 막는다.
- **두 손가락 핀치로 크기 조절**: 포인터를 `Map`으로 추적해 두 번째 손가락이 닿으면 이동 모드를 취소하고 핀치 모드로 전환. 손가락 사이 거리 변화(15px당 1단계)로 크기(1~5)를 실시간 반영하고, 손을 떼면 저장. 핀치가 있었던 제스처는 마지막 손가락을 떼도 탭으로 처리하지 않는다(미리보기가 잘못 열리는 것 방지).
- 관련 로직은 `js/ui/drag.js` 모듈로 분리 (이동 + 핀치 크기 조절 모두 포함).

**미리보기 (참고: 글하나_선택시_화면.png)**
- 원을 탭하면 해당 원이 커지며 **본문 앞 몇 줄 미리보기**를 원 안에 표시 ("글의 처음 몇 줄만 여기에 표시 됩니다...").
- 원 안 `+` 버튼을 누르면 상세보기 화면으로 이동.

**상세보기 (참고: 상세보기_화면.png)**
- 헤더 + **작성 날짜 표시** + 전체 본문 + 하단 액션 버튼: **수정 / 삭제 / 저장하기**.
- 삭제는 `--danger`, 저장하기는 candy-500(핑크), 수정은 앰버 버튼(`--btn-bg` / `--btn-text`).
- **지난 날짜의 기존 메모는 수정 불가** — 수정/저장하기 버튼을 아예 표시하지 않고 삭제만 가능. (새로 작성 중인 메모는 날짜와 무관하게 항상 편집 가능)
- 크기(중요도) 조절은 상세보기가 아니라 **메인화면에서 원을 두 손가락으로 핀치**해서 한다 (위 "메인 화면" 참고).
- **확정 상태(3단계) 선택**: 편집 모드에 실제 원 스타일 그대로 축소한 미니 원 3개(가능성 없음/생각중/확정)를 범례 겸 선택 버튼으로 표시. 탭하면 선택되고, 저장하기를 눌러야 반영된다.

**공통 규칙**
- 본문 텍스트 `--ink`, UI 텍스트 `--ui-text` 계열, 포인트는 화면의 5% 이내.
- 포커스 링은 `--focus-ring`(sky 500) 이상. 키보드 접근성 확보.

## 파일 구조

```
days/
├── index.html              → 앱 엔트리 포인트
├── manifest.webmanifest    → PWA 매니페스트
├── service-worker.js       → 오프라인 캐싱 (루트에 위치해 scope 전체 적용)
├── css/
│   ├── variables.css       → 컬러·타이포 CSS 변수 (라이트/다크)
│   ├── base.css            → 리셋·전역 기본 스타일
│   ├── layout.css          → 한줄 캘린더·메인·상세 레이아웃
│   └── components.css      → 메모 원·버튼·태그 등 컴포넌트
├── js/
│   ├── main.js             → 엔트리(type="module"), 초기화·화면 전환
│   ├── store.js            → localStorage CRUD (데이터 계층)
│   ├── memo.js             → 메모 모델·상태 판정(확정 상태 3단계/회색) 헬퍼
│   ├── views/
│   │   ├── mainView.js     → 떠다니는 메모 원 렌더링
│   │   └── detailView.js   → 상세보기·수정 화면
│   └── ui/
│       ├── calendarBar.js  → 한줄 캘린더 헤더
│       ├── drag.js         → 메모 원 드래그(Pointer Events, 위치 이동 + 두 손가락 핀치 크기 조절)
│       └── icons.js        → (미착수) Lucide SVG 인라인 아이콘 — 필요해질 때 생성
├── icons/                  → PWA 설치 아이콘 (192/512 등)
└── assets/                 → (미착수) 폰트·기타 정적 파일 — 폴더만 있고 내용 없음
```

---

## 코딩 규칙

### HTML
- 시맨틱 태그 우선 사용 (header, nav, main, article, section, footer)
- 주석은 한국어로 작성
- 들여쓰기는 2칸

### CSS
- 클래스명은 명확한 의미 전달
- Vanilla CSS 사용 (프레임워크 사용 금지)
- 주석으로 섹션 구분
- 컬러 시스템은 CSS 변수로 관리 (변수명 임의 변경 금지)

### JavaScript
- ES6+ 문법 사용
- 가독성을 위해 함수는 작게 분리
- 주석은 한국어로 작성

---

## 메모 데이터 구조

```js
{
  id: "uuid",            // 고유 ID (crypto.randomUUID())
  title: "메모 제목",     // 메인화면 원(뭉게구름)에 표시되는 제목
  content: "메모 내용",   // 본문 (상세보기에서 표시)
  date: "2026-07-27",    // 이 메모의 작성/예정 날짜(YYYY-MM-DD). 날짜와 무관하게 메인화면엔 항상 표시되고, 확정 상태(원 스타일)·지난 날짜(회색)로만 구분됨
  size: 3,               // 중요도 1~5 → 원 크기에 매핑 (메인화면에서 원을 두 손가락으로 핀치해 조절)
  xPct: 42,              // 메인화면 내 가로 위치(%). 0~100은 "홈" 화면 영역이고, 배경 좌우 패닝 기능으로 그 밖(음수·100 초과)에도 둘 수 있음. 드래그로 이동·저장
  yPct: 30,              // 메인화면 내 세로 위치(%, 0~100 고정 — 상하 패닝은 없음). 드래그로 이동·저장
  status: "thinking",    // 확정 상태 3단계: "unlikely"(회색 채움) / "thinking"(점선) / "confirmed"(실선). 상세보기 편집 모드의 미니 원 3개로 선택
  pinned: false,         // 중요 메모 고정 (선택 기능)
  createdAt: "2026-07-27T09:00:00.000Z", // 최초 작성 시각(ISO)
  updatedAt: "2026-07-27T09:00:00.000Z"  // 마지막 수정 시각(ISO)
}
```

- **회색(지난 메모) 판정**은 저장 필드가 아니라 `date < 오늘` 로 렌더링 시 계산(`memo.js`).
- **좌표(`xPct`, `yPct`)는 %로 저장** — px로 저장하면 기기마다 화면 폭이 달라 위치가 어긋남. 렌더링 시 컨테이너 크기에 곱해 실제 위치로 환산.
- 신규 메모의 초기 좌표는 겹치지 않도록 랜덤/오프셋 배치 후, 사용자가 드래그로 조정.
- localStorage 저장 키: `days.memos` (배열을 JSON.stringify).
- 스키마 변경에 대비해 저장 객체에 `version` 필드를 함께 두는 것 권장(예: `{ version: 1, memos: [...] }`).

## 전체 진행 상황

| 단계 | 내용 | 상태 |
|---|---|---|
| 1단계 | 프로젝트 기본 설정 | ✅ 완료 (2026-07-27) |
| 2단계 | 메모앱 기본 기능 (CRUD) | ✅ 완료 (2026-07-28, "검색 & 필터"는 Non-goals로 이동) |
| 3단계 | UI 확장 (PWA·다크모드·공유 등) | 🟨 진행 중 — PWA 필수만 완료, 나머지 미착수 |

---

## 1단계. 프로젝트 기본 설정

### 파일 구조 생성
- [x] 폴더 구조 생성 (`css/`, `js/`, `js/views/`, `js/ui/`, `icons/`, `assets/`) (2026-07-27)
- [x] `index.html` 엔트리 포인트 생성 (`<script type="module" src="js/main.js">`) (2026-07-27)
- [x] `css/variables.css` 생성 — 컬러 시스템 CSS 변수 선언 (2026-07-27)
- [x] `css/base.css` 리셋·전역 기본 스타일 (2026-07-27)

### Git 설정
- [x] `git init` 및 `.gitignore` 작성 (2026-07-27)
- [x] GitHub 원격 저장소 연결 (2026-07-27)
- [x] 최초 커밋 (2026-07-27)

---

## 2단계. 메모앱 기본 기능 (CRUD)

### 메인화면 디자인 초안 잡기
- [x] "한줄 캘린더" 헤더 컴포넌트 (`js/ui/calendarBar.js`) (2026-07-27)
- [x] 떠다니는 메모 원(뭉게구름) 레이아웃 (`js/views/mainView.js`) (2026-07-27)
- [x] 중요도(size) → 원 크기 매핑 (2026-07-27)
- [x] 확정 상태 3단계(가능성 없음/생각중/확정) · 회색(지난 날짜) 상태 표현 (2026-07-27, 3단계로 확장 2026-07-28)
- [x] 원 테두리를 5배 두껍게 (2026-07-28)
- [x] 마지막으로 터치한 원이 항상 맨 위로 오도록 (2026-07-28)
- [x] 읽기 전용 텍스트(원 제목, 상세보기 수정 전 제목·본문) 길게 눌러도 선택/사전 찾기 팝업 안 뜨도록 (2026-07-28)
- [x] 메인화면 배경을 손가락으로 끌어도 브라우저 기본(네이티브) 좌우 팬이 발생하지 않도록 원천 차단 — 대신 아래 커스텀 패닝으로 대체 (2026-07-28)
- [x] 배경을 좌우로 밀어서(패닝) 화면 밖 영역에 원을 두고 다시 밀어서 찾아올 수 있는 기능 (2026-07-28)
- [x] 헤더 왼쪽 상단에 "days" 로고 배치 (2026-07-27)

### 원 드래그 (위치 이동)
> 데이터의 `xPct`/`yPct` 필드와 저장 로직이 함께 걸리는 기능. UI·동작·저장을 한 번에 묶어 진행.
- [x] `js/ui/drag.js` — Pointer Events 기반 드래그 (마우스·터치·펜 공용) (2026-07-27)
- [x] `touch-action: none` + `setPointerCapture`로 모바일 드래그 안정화 (2026-07-27)
- [x] `transform: translate`로 위치 갱신, 부유 애니메이션과 `.dragging` 클래스로 조율 (2026-07-27)
- [x] 드래그 종료 시 `xPct`/`yPct` 계산 후 `store.save()`로 localStorage 반영 (2026-07-27)
- [x] 탭(미리보기)과 드래그(이동) 임계값(약 6px)으로 구분 (2026-07-27)
- [x] 두 손가락 핀치로 크기(중요도) 조절 — 다른 메모와 비교하며 메인화면에서 직접 조절 (2026-07-27)
- [x] 최대 크기(중요도 5)는 화면을 넘칠 만큼 크게 설정 (2026-07-28)
- [x] 핀치 조작 시 브라우저 자체의 페이지 확대/축소가 절대 발생하지 않도록 원천 차단 (2026-07-28)

### 배포하기 (초안 확인용)
- [x] Vercel 프로젝트 연결 (2026-07-27)
- [x] 초안 배포 및 URL 확인 (배포 URL을 개요에 기입) (2026-07-27)

### 메모 저장/불러오기
- [x] `js/store.js` — localStorage 저장/불러오기 모듈 (2026-07-27)
- [x] 데이터 구조(version + memos) 초기화 로직 (2026-07-27)

### CRUD 기능
- [x] 메모 추가 — 오른쪽 하단 플로팅 `+` 버튼(FAB) → 상세보기를 편집 모드로 바로 열어 작성 (2026-07-27)
- [x] 캘린더에서 날짜를 선택하면 그 날짜로 메모 작성 가능(미래 날짜 포함) (2026-07-27)
- [x] 미리보기 (원 탭 시 본문 앞 몇 줄 표시) (2026-07-27)
- [x] 상세보기 화면 (`js/views/detailView.js`) — 본문 + 수정/삭제/저장하기 + 작성일 표시 (2026-07-27)
- [x] 메모 수정 (2026-07-27)
- [x] 메모 삭제 (`--danger`, 삭제 전 확인) (2026-07-27)
- [x] 확정 상태 3단계 선택 UI — 편집 모드에 미니 원 3개(범례 겸 선택 버튼) (2026-07-28)
- [x] 지난 날짜의 기존 메모는 수정 불가 — 삭제만 가능 (2026-07-27)

---

## 3단계. UI 확장

### PWA 필수
- [x] `manifest.webmanifest` 작성 (이름·아이콘·theme_color 등) (2026-07-27)
- [x] `service-worker.js` — 오프라인 캐싱 (2026-07-27)
- [x] 홈 화면 설치 지원 확인 (2026-07-27)

### 추가 기능 3가지
- [ ] 다크모드 (`data-theme` 전환, 역할 변수 재정의 / 다크 값 실기기 확정)
- [ ] 클립보드 복사
- [ ] 공유 기능 (Web Share API)

### 선택 기능
- [ ] 메모 개수 표시
- [ ] 중요 메모 고정 (`pinned`)

### 백업
- [ ] JSON 파일 내보내기 / 가져오기
